# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# gem "bubbles"
gem "glamour"
gem "harmonica"
gem "lipgloss"

# Local development (monorepo)
#
# Prefer Bundler's local override instead of hardcoding `path:` dependencies:
#   bundle config set local.bubbles ../bubbles-ruby
#   bundle config set local.glamour ../glamour-ruby
#   bundle config set local.harmonica ../harmonica-ruby
#   bundle config set local.lipgloss ../lipgloss-ruby
#   bundle config set local.charm-native ../charm-native

gem "irb"
gem "logger"
gem "maxitest", "~> 7.0"
gem "rake", "~> 13.0"
gem "rake-compiler", "~> 1.3"
gem "rake-compiler-dock"
gem "rubocop"
